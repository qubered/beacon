apiVersion: 1
providers:
  - name: Beacon
    folder: Beacon
    type: file
    # Packs carry their own dashboards, so installing a Pack brings its board
    # with it (spec §12). Core writes them into this directory.
    options: { path: /var/lib/grafana/dashboards }
