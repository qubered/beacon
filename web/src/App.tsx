export function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem", maxWidth: "40rem" }}>
      <h1>Beacon</h1>
      <p>
        Pre-implementation. See <code>docs/ROADMAP.md</code> — the status wall lands in
        milestone M3, the builder in M6.
      </p>
    </main>
  );
}
